# Copy this file to .env and fill in your Google API key
# Only needed when running the server standalone (npm run start:server)
# When using the Electron app, set the key in the Setup screen instead

GOOGLE_API_KEY=your_google_api_key_here
PORT=3000
